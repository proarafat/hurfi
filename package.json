{
  "name": "hurfi",
  "version": "1.0.0",
  "private": true,
  "description": "Hurfi — B2B Growth & Digital Presence Partner website",
  "scripts": {
    "build": "node scripts/patch-pages.mjs && node scripts/generate-seo.mjs",
    "seo": "node scripts/generate-seo.mjs",
    "patch": "node scripts/patch-pages.mjs",
    "dev": "npx --yes serve ."
  },
  "engines": {
    "node": ">=18"
  }
}
